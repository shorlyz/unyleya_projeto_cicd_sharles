trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'Docker Hub Connection' # Criar service connection antes
    repository: 'shorlyz/unyleya_projeto_cicd_sharles_sa'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: 'latest'